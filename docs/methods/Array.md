# ```Array``` type methods

## ```length```
The ```length``` method returns the length of an array as a ```usize```.

```Array.length(self: Array) -> usize```

```
let arr = [1,2,3]
arr.length() // == 3usize
```